# Memory Bank Index

Quick-reference pointers into the memory bank. Read the file itself for details.

Sections below are created dynamically by the `sync-memory-bank` agent based on the project's language, framework, and architecture. Do not assume a fixed set of sections — they should reflect what this specific project actually contains.

Each `## Section Name` header corresponds to a directory in `.llm/memory/`. Documents within each section have one-line summaries.
